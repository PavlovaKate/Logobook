// import type { AxiosResponse } from 'axios';
// import { request } from '../../services/axiosInstance';
// import { Book, BookId } from '../Main/type/type';

// export const axiosUpdateFavourite = async (id: BookId): Promise<{ message: string; books: Book[] }> => {
//   const response: AxiosResponse<{ message: string; books: Book[] }> =
//     await request.get(`/favorites/${id}`);
//   return response.data;
// };

